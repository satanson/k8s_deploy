apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-fast-1g
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: fast
