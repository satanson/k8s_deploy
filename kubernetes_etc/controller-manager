###
# The following values are used to configure the kubernetes controller-manager

# defaults from config and apiserver should be adequate

# Add your own!
KUBE_CONTROLLER_MANAGER_ARGS="--root-ca-file=/opt/kubernetes/tls/ca.pem --service-account-private-key-file=/opt/kubernetes/tls/apiserver-key.pem --pod-eviction-timeout=300s --kubeconfig=/opt/kubernetes/etc/kubeconfig.conf"
